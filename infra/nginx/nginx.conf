events {}
http {
  # docker 내장 DNS로 매 요청 app 인스턴스 재해석 → --scale app=N 시 라운드로빈
  resolver 127.0.0.11 valid=5s;
  server {
    listen 80;
    location / {
      set $target http://app:8080;
      proxy_pass $target;
      proxy_set_header X-Forwarded-For $remote_addr;
    }
  }
}
